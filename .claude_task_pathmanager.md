# 任务: PathManager 通过 IPC 真正工作

## 背景
AISafeOS64 微内核操作系统需要将 PathManager 从一个独立框架改造成通过 IPC endpoint 在 EL0 用户态真正运行的服务。

## 现有代码架构

### 内核 IPC 系统（已验证可用）
- 系统调用: `SYS_EP_CREATE(0x010A)`, `SYS_MSG_SEND(0x0104)`, `SYS_MSG_RECV(0x0105)`, `SYS_MSG_REPLY(0x0106)`
- EL0 通过 `svc #0` 触发系统调用，参数通过 x0-x5 传递，x8=系统调用号
- 现有 3 个 EL0 服务 (FS/Proc/Mem) 已通过 `create_service_thread()` 在 QEMU 中验证通过
- 客户端通过 `g_service_eps[]` 获取服务端 endpoint ID，然后 `SYS_MSG_SEND/RECV` 通信

### 系统调用约定（EL0 用户态）
```c
// 在 EL0 用户态代码中使用（通过 svc #0 触发）
// x8 = 系统调用号, x0-x5 = 参数, x0 = 返回值
static inline int64_t syscall0(uint64_t nr);
static inline int64_t syscall2(uint64_t nr, uint64_t a0, uint64_t a1);
static inline int64_t syscall3(uint64_t nr, uint64_t a0, uint64_t a1, uint64_t a2);
```

这些 syscallN 函数定义在 `include/kernel/syscall.h` 中（通过内联汇编实现 svc #0）。

### entry.c 中现有的 EL0 服务模式（参考 fs_service_entry）
```c
static void fs_service_entry(void *arg)
{
    (void)arg;
    int64_t ep_id;
    uint8_t recv_buf[32U];

    /* 打印启动标记 */
    (void)syscall2(SYS_DEBUG_PRINT, (uint64_t)(uintptr_t)"[FS] RUNNING\n", 12ULL);

    /* 创建 endpoint */
    ep_id = syscall2(SYS_EP_CREATE, 0ULL, 0ULL);
    if (ep_id <= 0) { for (;;) { } }

    /* 注册到服务表 */
    g_service_eps[0U] = (uint64_t)ep_id;

    /* 接收一次请求 */
    (void)syscall3(SYS_MSG_RECV, (uint64_t)ep_id, (uint64_t)(uintptr_t)recv_buf, 32ULL);

    /* 回复客户端 */
    (void)syscall3(SYS_MSG_REPLY, (uint64_t)ep_id, (uint64_t)(uintptr_t)"FS_OK", 5ULL);

    syscall0(SYS_THREAD_YIELD);
    for (;;) { }
}
```

### 现有服务注册
- `g_service_eps[0]` = FS 服务
- `g_service_eps[1]` = Proc 服务
- `g_service_eps[2]` = Mem 服务
- **`g_service_eps[3]` = 空闲，分配给 PathManager**

### MAX_SERVICES 常量
检查 entry.c 中 `g_service_eps` 的定义，确保 MAX_SERVICES >= 4。如果不是，需要增加。

### 客户端 user_test_entry
参考 entry.c 中的 `user_test_entry()` 函数，它：
1. 等待所有服务 endpoint 注册完成
2. 依次向每个服务发送测试消息
3. 验证回复并打印结果

## 需要修改的文件

### 1. `kernel/arch/arm64/entry.c`

#### a) 添加 PathManager 服务入口
```c
/**
 * @brief PathManager 服务入口函数
 *
 * @details 创建 endpoint → 注册到 g_service_eps[3] →
 *          消息循环 (RECV → 解析 → 处理 → REPLY)
 *          支持: REGISTER / UNREGISTER / LOOKUP / ENUMERATE
 */
static void path_service_entry(void *arg)
```

PathManager 需要：
- 内部路径表（静态数组，EL0 可访问）
- 消息循环：不断 RECV，根据消息头 4 字节判断类型，处理后 REPLY
- 四种消息类型的处理逻辑

#### b) 消息协议
```
路径管理器消息协议（所有消息统一格式）:

偏移 0:  msg_type (uint32_t) — 消息类型
偏移 4:  数据区（取决于消息类型）

PATH_MSG_REGISTER (0x0030):
  请求 [0]:  msg_type = 0x0030
  请求 [4]:  path_type (uint32_t)
  请求 [8]:  service_id (uint32_t)  
  请求[16]:  endpoint_id (uint64_t)
  请求[24]:  path 字符串 (PATH_NAME_MAX=64 字节)
  请求总大小: 88 字节
  回复 [0]:  status (int32_t) — 0 成功, 负数错误码

PATH_MSG_UNREGISTER (0x0031):
  请求 [0]:  msg_type = 0x0031
  请求 [4]:  path 字符串 (64 字节)
  请求总大小: 68 字节
  回复 [0]:  status (int32_t)

PATH_MSG_LOOKUP (0x0032):
  请求 [0]:  msg_type = 0x0032
  请求 [4]:  path 字符串 (64 字节)
  请求总大小: 68 字节
  回复 [0]:  status (int32_t)
  回复 [8]:  endpoint_id (uint64_t)

PATH_MSG_ENUMERATE (0x0033):
  请求 [0]:  msg_type = 0x0033
  请求 [4]:  path_type (uint32_t)
  请求总大小: 8 字节
  回复 [0]:  count (uint32_t)
  回复 [8]:  endpoint_ids[0..7] (8 × uint64_t)
```

#### c) PathManager 数据结构
```c
#define PATH_MAX_ENTRIES  16U
#define PATH_NAME_MAX     64U

typedef struct {
    char            path[PATH_NAME_MAX];
    uint32_t        type;           /* PATH_TYPE_SERVICE=0, PATH_TYPE_DEVICE=1 */
    uint32_t        service_id;
    uint64_t        endpoint_id;
    uint32_t        flags;
} path_entry_el0_t;

static path_entry_el0_t s_pm_table[PATH_MAX_ENTRIES];
static uint32_t s_pm_count;
```

#### d) 添加栈空间
PathManager 需要独立的内核栈和用户栈，类似现有的 s_fs_kern_stack 等：
```c
static uint64_t s_path_kern_stack[1024] __attribute__((aligned(16)));
static uint64_t s_path_user_stack[2048] __attribute__((aligned(16)));
```

#### e) 在 kernel_main 中创建线程
在现有服务创建代码之后添加：
```c
(void)create_service_thread(path_service_entry, "path_svc",
                            s_path_kern_stack, s_path_user_stack,
                            2048U * sizeof(uint64_t) / sizeof(uint64_t),
                            (priority_t)48U);
```

#### f) 扩展 user_test_entry 添加 PathManager 测试
在客户端的测试循环中，添加对 PathManager 的测试：
1. 等待 g_service_eps[3] 非零
2. 发送 PATH_MSG_REGISTER 注册 "/services/test_svc"
3. 等待 REPLY，验证 status=0
4. 发送 PATH_MSG_LOOKUP 查找 "/services/test_svc"
5. 等待 REPLY，验证 status=0 且 endpoint_id 匹配
6. 打印 `[EL0] PATH OK`

## 代码规范
- **MISRA C:2012**: 严格合规
- **4 空格缩进, Allman 括号风格**
- **中文 Doxygen 注释** (所有公共函数)
- **每行最多 120 字符**
- **圈复杂度 <= 10**
- 所有变量在函数开头声明（C89 风格）
- uint8_t/uint32_t 等标准类型
- 禁止使用标准库（EL0 无标准库），所有字符串操作手写

## 验收标准
1. `path_service_entry()` 在 EL0 创建 endpoint 并注册到 g_service_eps[3]
2. 消息循环处理 REGISTER/UNREGISTER/LOOKUP/ENUMERATE 四种消息
3. 客户端通过 IPC 注册+查找测试服务，QEMU 输出 `[EL0] PATH OK`
4. 现有测试 (CAP TEST, SMP TEST, DRV TEST) 不回归
5. text 段增长 < 5KB
6. 编译通过（ARM64 交叉编译）

## 构建和验证命令
```bash
cd /home/kerfs/AISafeOS64/AISafeOS64
mkdir -p build && cd build
cmake .. -DCMAKE_TOOLCHAIN_FILE=../cmake/toolchain-arm64.cmake
make -j$(nproc)

# 检查 text 段大小
aarch64-linux-gnu-size kernel/aisafe64.elf.elf

# QEMU 验证（10秒超时）
timeout 10 qemu-system-aarch64 -M virt -cpu cortex-a57 -smp 4 -m 1G \
  -kernel kernel/aisafe64.elf.elf -nographic -serial mon:stdio 2>&1 | head -200
```

## 重要注意事项
1. entry.c 是一个大文件 (~2289 行)，所有修改必须精确定位
2. 所有新的 EL0 代码都在 `#if CONFIG_DEBUG` 块内
3. syscallN 函数在 EL0 用户态代码中才可用
4. 消息缓冲区大小建议 256 字节（容纳最大的 ENUMERATE 回复）
5. PathManager 必须先于客户端启动（更高优先级），所以 prio=48 优于客户端的 prio=50
6. 确保在 services 之前创建 PathManager，这样客户端查找时服务已注册
7. path_service_entry 必须在 entry.c 内部定义（不是 services/path/main.c），因为 EL0 代码需要在内核映像中

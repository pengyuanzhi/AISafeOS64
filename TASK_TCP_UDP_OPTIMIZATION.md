# 任务：TCP/UDP 优化实现

## 背景
AISafeOS64 网络协议栈已具备完整框架，但以下功能待优化：
1. TCP 定时器集成（重传、Keepalive、RTT 浄量）
2. TCP 确认号处理优化（ACK 重复检测、ACK 滑动窗口）
3. UDP 校验和实现

## 目标
实现这三个功能模块，确保功能正确性和性能优化。

## 技术要求

### 1. TCP 定时器集成

**实现内容**：
- TCP 重传定时器处理 (`tcp_retransmit_check()`)
- TCP Keepalive 定时器处理 (`tcp_keepalive_check()`)
- RTT 测量 (`tcp_rtt_t`)
- 定时器驱动的主循环集成

**核心功能**：
- 每隔 10ms 检查一次 TCP 重传队列
- 超过最大重传次数 (5次) 的段自动丢弃
- Keepalive 每 75 秒检查一次，2 小时无数据后启动
- RTT 测量使用 TCP Timestamp 选项

**代码位置**：`services/net/main.c`

### 2. TCP 确认号处理优化

**实现内容**：
- ACK 重复检测（检测重复 ACK）
- ACK 滑动窗口处理
- ACK 丢包重传（快速重传）

**核心功能**：
- 跟踪已确认的序列号
- 识别重复 ACK（duplicate ACKs）
- 3 个重复 ACK 触发快速重传

**代码位置**：`services/net/main.c`

### 3. UDP 校验和实现

**实现内容**：
- UDP 头部校验和计算
- UDP 数据包接收时校验和验证
- 伪首部构造

**核心功能**：
- 计算完整的 UDP 校验和（包括伪首部）
- 接收时验证校验和
- 校验和为 0 表示禁用校验和

**代码位置**：`services/net/main.c`

## 开发规范

### TDD 流程
1. **RED**：先写测试（如果需要）
2. **GREEN**：实现最小代码
3. **REFACTOR**：优化代码质量

### 代码规范
- MISRA C:2012 合规
- 4 空格缩进，Allman 括号风格
- 中文注释
- 函数长度 < 50 行
- 圈复杂度 < 10

### 提交规范
- 自动提交
- Conventional Commits (中文描述)
- 包含详细说明和验证结果

## 测试验证

### 测试项目
1. TCP 重传定时器测试
2. TCP Keepalive 定时器测试
3. RTT 测量测试
4. ACK 重复检测测试
5. UDP 校验和计算测试
6. UDP 校验和验证测试

### 验证方法
- 编译验证
- 代码审查
- 静态分析

## 预期成果

1. ✅ TCP 重传定时器正确工作
2. ✅ TCP Keepalive 正确工作
3. ✅ RTT 测量正确实现
4. ✅ ACK 重复检测正确工作
5. ✅ UDP 校验和正确计算和验证
6. ✅ 定时器集成到主循环
7. ✅ 所有测试通过

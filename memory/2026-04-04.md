# 2026-04-04 开发日志

## P0 多核负载测试验证完成 ✅ (19:35-20:30)

### 完成项
1. **多核负载测试** — 4 worker 线程分布 4 核并行运行
2. **SMP 负载均衡验证** — `smp_sched_init()` + `smp_set_affinity()` 绑核工作正常
3. **多核 tick 心跳** — 每 CPU 独立定时器中断 + UART 自旋锁串行化打印

### 关键变更 (commit: `eb42c40`)
- **entry.c**: 添加 UART TicketLock (`g_uart_lock`)、`smp_uart_putc/puts`、`smp_worker_entry` 4 个 worker 线程（每核一个，300ms 周期打印 CPU ID 数字）
- **smp.c**: `smp_sched_init()` 移除 memset（用 volatile 手动清零，避免裸机 libc 依赖），初始化每 CPU 就绪队列
- **scheduler.c**: 添加 tick 计数打印 `[tick=N]`
- **timer.c**: tick handler 调用 `smp_send_reschedule()`

### QEMU 验证结果
- `Online CPUs: 0x0000000000000004` — 4 核全在线
- 输出 `AB3210` — 线程 A(100ms) + B(200ms) + worker 0-3 正常交错
- text = 25.5KB (< 30KB ✅)
- 19/19 宿主单元测试全通过

### commit 链
- `eb42c40` feat(sched,smp): 多核负载测试+负载均衡+tick心跳验证
- `fd12b72` docs: 更新 P0 多核验证完成+项目进度85%

---

## 用户请求: MMU 页表映射启用 (22:34)

- 方成提出下一步要做 **MMU 页表映射启用**
- 已读取 boot.S 和 smp_boot.c 分析当前 MMU 状态
- 任务待执行（会话被压缩前未完成）

### 当前 MMU 相关代码现状
- **boot.S**: 包含 `__enable_mmu` 函数框架（仅注释，未实现）
- **链接脚本**: 内核基地址 `0x40080000`，页大小 4KB
- 关键符号: `__kernel_base`, `__rodata_end`, `__bss_start`, `__bss_end`, `__stacks_start`, `__stacks_end`, `__heap_start`, `__heap_end`, `__kernel_end`
- **smp_boot.c**: 从核启动使用 HVC PSCI，当前运行在物理地址（identity mapping）

### 待做
- [ ] 实现 ARM64 页表映射（TTBR0_EL1/TTBR1_EL1）
- [ ] kernel text段只读+执行权限
- [ ] rodata 只读权限
- [ ] bss/data 读写权限
- [ ] 设备内存 device 属性（nGnRnE）
- [ ] 从核 secondary 页表切换
- [ ] QEMU 验证 MMU 启用后内核正常运行

---

## 项目进度: ~85%

### 当前 text 大小: 25.5KB (目标 < 30KB)
